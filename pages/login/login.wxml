<!--pages/login/login.wxml-->
<view class="full-height">
  <view class="logo-box">
    <image mode="aspectFit" src="./logo_transparent.png" />
  </view>
  <view>
    <!-- <van-button disabled="{{ loading }}" custom-class="weixin-quick-login-btn" block bindtap="onLoginClick" >微信账号快捷登陆</van-button> -->
    <!-- <van-cell value-class="login-by-mobile-btn" value="输入手机号码登陆/注册" link-type="navigateTo" url="/pages/login/mobile-login" 
    border="{{ false }}" /> -->
    <van-cell is-link title="国家与地区" value="{{ countryCode }}" link-type="navigateTo" url="/pages/login/area-code-picker" />
    <van-field value="{{ mobile }}" label="手机号" clearable  maxlength="11" placeholder="请输入手机号" error-message="{{ mobileErrorMessage }}" bind:change="onMobileChange"/>
    <van-field value="{{ password }}" label="密码" password maxlength="50" clearabel placeholder="请输入密码" bind:change="onPasswordChange"></van-field>
    <!-- <van-field value="{{ smsVerificationCode }}" label="短信验证码" clearable placeholder="请输入短信验证码">
      <van-button slot="right-icon" class="custom-icon" bind:click="onGetSmsVerificationCode" 
      disabled="{{ disableGetSmsCodeBtn }}">获取验证码 
      <view wx:if="{{ countDown }}">
        {{countDown}}
      </view>
      </van-button>
    </van-field> -->
    <van-button block bind:tap="onLoginClick">登陆</van-button>
    <van-button block bind:tap="onSignUpClick">新用户注册</van-button>
  </view>
  <van-toast id="van-toast" />
</view>